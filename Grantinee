on "grantinee_development", :mysql do
  # User on any host
  user :username do
    select :users, [ :id, :anonymized ]
    insert :users, [ :anonymized ]
    update :users, [ :anonymized ]
    
    select :lists_users
    insert :lists_users
    update :lists_users
  end
end
